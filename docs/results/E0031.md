# E0031: corrected same-run parent-event ranker

E0031 corrects E0029's invalid cross-run node-ID join. Candidate generation, embryo-held-out daughter-pair ranking, raw-image and DeepCenter feature extraction, and embryo-held-out parent classification all operate on the same inference graph.

The run uses histogram gradient boosting for the parent-event classifier. It is diagnostic and does not mutate the graph.

## Result

Invalid. Although candidate generation and extraction ran in one notebook, the candidate audit used scorer-reindexed node IDs while feature extraction looked up original pipeline IDs. Only 72,406 parents and nine of 16 positives survived, exactly reproducing E0029's invalid subset and metrics. No model conclusion is drawn from this run.
