# EXT0004: verified claimed leaderboard 0.933 anchor

The downloaded notebook `933-biohub-bidir30.ipynb` is a fork of the verified 0.931 production anchor with two executable parameter changes:

- `BIOHUB_BIDIRECTIONAL_EDGE_WEIGHT`: `0.15` to `0.30`
- `BIOHUB_SECONDARY_DETECTION_WEIGHT`: `0.475` to `0.80`

The notebook's comment describes a single change, but normalized source comparison confirms both changes plus their bidirectional guard updates. The user reports a post-reset leaderboard score of `0.933`. Until independently submitted by the team, it is treated as a provisional external leaderboard anchor.

SHA-256: `3909141a792a91e40f1ef8a54939b27b5dd5b614abe891c0f6acc78bb3d7bc04`.
