# E0032: corrected same-run ExtraTrees parent ranker

E0032 shares E0031's corrected same-run candidate and feature pipeline, then evaluates an independent ExtraTrees parent-event classifier. Comparing the two models determines whether any parent-event signal is robust to classifier choice.

The run is diagnostic and does not mutate the graph.

## Result

Invalid for the same namespace mismatch as E0031. It reproduced E0030's nine-positive subset and metrics exactly. E0033/E0034 rebuild the feature lookup from the scorer-reindexed graph used by candidate generation.
