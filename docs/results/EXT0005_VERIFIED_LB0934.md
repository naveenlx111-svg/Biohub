# EXT0005: verified leaderboard 0.934 anchor

Team submission `55931557` completed on 2026-09-01 with public score
`0.934` after the evaluator reset. Its Kaggle source was
`biohub-c35-fallback-detection-cleanroom-20260831`, version 1. This supersedes
EXT0004 (`0.933`) as the verified team leaderboard anchor.

The exact private notebook output is not retrievable through the current
Kaggle CLI session-output endpoint, which returns HTTP 404 even though the
submission and score are visible through the authoritative competition
submissions endpoint. The leaderboard score is therefore verified, while a
source-level local reproduction remains pending.
