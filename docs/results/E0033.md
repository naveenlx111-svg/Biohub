# E0033: official audit of the 0.933 anchor

E0033 freezes the post-reset 0.933 configuration, now leaderboard-verified by submission `55889101`, and runs the patched official scorer, division failure anatomy, and corrected localization analysis on the same twelve-movie panel used for the previous 0.931 anchor.

The only production differences from E0023 are bidirectional edge weight `0.30` and secondary detection fusion `0.80`. This establishes whether the leaderboard gain is visible locally and how it changes edge, node, and division errors.

## Result

E0033 scored `0.936854` on the patched official twelve-movie validator, compared with `0.926608` for the 0.931 anchor. Adjusted edge Jaccard improved from `0.914108` to `0.918336`, and division Jaccard improved from `0.125000` to `0.185185`.

Division counts improved from `4 TP / 14 FP / 14 FN` to `5 TP / 9 FP / 13 FN`. Ten remaining misses have parent and daughter evidence but no parent-side fork; three lack required evidence. Node recall remained essentially flat at `0.981579` versus `0.981668`.

The combined local gain is `+0.010247`, with simultaneous edge and division improvements. This is the strongest submission-valid local configuration measured so far and independently supports promoting the reported 0.933 notebook to the production anchor.
