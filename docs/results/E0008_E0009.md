# E0008-E0009: association fusion sweep on SDW75

## Parent

Both arms use E0005/SDW75, which achieved adjusted edge Jaccard 0.9159 on the four-sample validator. Its secondary detection fusion weight is 0.75.

## E0008: secondary association weight 0.25

Change only `BIOHUB_SECONDARY_EDGE_WEIGHT` from 0.15 to 0.25. The secondary model is independently seeded and is already used for detection fusion. This arm gives its association logits more influence only in the existing low-margin-consensus path; high-confidence or disagreeing assignments remain protected by that gate.

## E0009: bidirectional harmonic weight 0.40

Change only `BIOHUB_BIDIRECTIONAL_EDGE_WEIGHT` from 0.30 to 0.40, including the matching runtime integrity guard. This makes low reverse-time support penalize a forward candidate more strongly before secondary-model association fusion.

## Decision rule

Compare weighted adjusted edge Jaccard against E0005's 0.9159 and inspect all four per-sample deltas. Division values from the embedded notebook remain obsolete proxies. Promote only a consistent edge improvement and verify that the generated production submission is complete.
